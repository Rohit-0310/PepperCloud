// const express = require('express');

// const Form = require("../models/form.model");


// const crudController = require("./crud.controller");

// const router = express.Router();

// router.post("", crudController.post(Form));
// router.get("", crudController.getAll(Form));
// router.get("/:id", crudController.getOne(Form));
// router.patch("/:id", crudController.updateOne(Form));
// router.delete("/:id", crudController.deleteOne(Form));


// module.exports = router;