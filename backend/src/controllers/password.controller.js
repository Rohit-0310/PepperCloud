// const express = require('express');

// const Password = require("../models/password.model");


// const crudController = require("./crud.controller");

// const router = express.Router();

// router.post("", crudController.post(Password));
// router.get("", crudController.getAll(Password));
// router.get("/:id", crudController.getOne(Password));
// router.patch("/:id", crudController.updateOne(Password));
// router.delete("/:id", crudController.deleteOne(Password));


// module.exports = router;